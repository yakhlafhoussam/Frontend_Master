WEBVTT

00:00.000 --> 00:06.720
Cool. So classes can have class methods on them. Those are methods that are shared among

00:06.720 --> 00:16.320
all the instances of a certain type. They can be overwritten in a specific instance or sub-class.

00:16.880 --> 00:22.240
And in order to do that, you have to add what's called an annotation over that method.

00:22.960 --> 00:29.600
Called class method to say that this method does not expect an instance on it.

00:30.880 --> 00:38.800
Instead, you get a variable called class or CLS, whatever you want to call it.

00:39.760 --> 00:46.080
You should not name it CLSS because that is a keyword. So you might see something like this.

00:46.160 --> 00:53.120
If you see this, you might also see it called CLAZZ just so that it doesn't override this keyword here.

00:54.400 --> 00:59.600
And this lets you access some of the variables that are defined in the class. So now I can

00:59.600 --> 01:10.880
access class.runs and class.number of wheels. So I know that all of my car classes are going to have

01:10.960 --> 01:16.080
four wheels. And so this method can be shared between all of the instances.

01:18.560 --> 01:25.440
We can, though, just override that in our instance itself. If I make a new my car instance,

01:25.440 --> 01:30.080
I can set number of wheels to six. That will only affect that one instance.

01:30.480 --> 01:40.480
Now, just like we used type on the Ripple, there are two more helper methods that you can use.

01:40.480 --> 01:52.320
Is instance and is subclass to tell you what if a particular variable is an instance of a particular class.

01:53.200 --> 02:05.440
So for example, let's try these out. I can ask Python, if I know the type of 42,

02:06.480 --> 02:16.320
and I know the type of a string, I can ask Python is instance 42 int and I should get true.

02:16.640 --> 02:30.080
Can I also ask is instance the string high int? No. Is instance high stir this will return true?

02:31.680 --> 02:35.920
And I can do the same thing for the objects that I define.

02:36.800 --> 02:55.200
So here, let's exit out of the Ripple. I can print out is instance my other car,

02:55.280 --> 03:06.480
car, and run this, and we'll see that it is true. So if you need to investigate what's going on

03:06.480 --> 03:12.800
with your objects or your instances, is instance and is subclass will help you figure out those answers.

03:16.000 --> 03:22.480
One other method I want to talk to you about in classes is this optional magic method called

03:23.360 --> 03:32.000
we haven't talked about magic methods just yet. Magic methods are defined by having two underscores

03:32.000 --> 03:39.040
before and after them. That means that there's something special about these functions, these methods.

03:40.080 --> 03:48.080
So in this case, Dunder in it, Dunder stands for double underscores, is going to get run when you

03:48.080 --> 03:56.880
create an instance of that class. And you can do that Dunder in it method. You can use that method

03:56.880 --> 04:03.520
to define any scenarios or set up that you need when an instance is being created,

04:03.520 --> 04:10.640
including setting instance variables so that you can save what is passed into that constructor

04:10.640 --> 04:20.240
to use for later. So let's copy this and take a look at it.

04:27.680 --> 04:33.840
This is the my car class just like we had, but I've added a Dunder in it method here.

04:34.720 --> 04:42.240
Remember it's an it's axon and instance. So I need to pass in self first. I can also pass in

04:42.640 --> 04:48.640
make and a model. These are required arguments. So if I don't pass these in,

04:48.640 --> 04:57.120
instantiation will fail. Now if I want to save those values to use later, I need to save them

04:57.120 --> 05:04.560
on the instance just like this. Once I save them on the instance, that means that I can use them

05:04.560 --> 05:12.000
later on when I call other methods on them. So here when I call my car equals car forward and

05:12.000 --> 05:19.120
Thunderboard and I call start. I will print out that my forward Thunderbird is started

05:19.280 --> 05:29.120
room room. There are other magic methods that you might come across. One is

05:29.120 --> 05:36.800
Thunderster, the other is Thunderrepper and these are two handy magic methods that are useful for

05:36.800 --> 05:45.040
debugging. You can think of and they both return a string. You can think of Thunderster as the

05:45.040 --> 05:51.120
representation of your object that should be human readable. Something that you can pass back to

05:51.120 --> 05:58.640
the end user to let them know how this object is represented. Whereas Thunderrepper should return

05:58.640 --> 06:05.600
the Python code that's maybe necessary to rebuild the object. So Thunderrepper is used more for

06:05.600 --> 06:11.600
under the hood, a representation for other programmers, a representation for debugging. And that's

06:11.600 --> 06:21.120
kind of the difference. If you take a look at what is returned from date time dot date time dot now,

06:21.120 --> 06:28.320
which just creates a new object with the current date and time. If you take a look at the

06:28.320 --> 06:34.320
store representation, you'll see that there's a date and a timestamp. If you take a look at the

06:34.320 --> 06:40.160
wrapper, it will let you know that this is a date time dot date time. And these are the arguments

06:40.160 --> 06:47.200
that you would pass into that method in order to recreate it. These are just conventions. You

06:47.200 --> 06:54.240
don't have to follow them, but you will see these throughout Python code. Generally, you do want

06:54.240 --> 07:03.600
to have a stir and a wrapper in each of your classes. Know that you can also instantiate objects

07:03.600 --> 07:16.960
in four loops or in less comprehension. So a lot of these concepts are mixed and matched.

